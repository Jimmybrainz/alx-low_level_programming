#include <stdio.h>

/**
 * main - program to prints the alphabet in lowercase
 * Return: 0(success)
 */

int main(void)
{
	int y;

	for (y = 'a'; y <= 'z'; y++)
	{
		putchar(y);
	}
	putchar('\n');


	return (0);
}

